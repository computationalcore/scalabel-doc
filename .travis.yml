language: python
python:
  - "3.6"
before_install:
  - sudo apt-get update
  - sudo apt-get install -y npm
install:
  - sudo pip install --upgrade pip
  - sudo pip install pycodestyle
script: 
  "echo \"TODO\""
deploy:
  provider: s3
  access_key_id: access_key_id
  secret_access_key: secret_access_key
  bucket: "S3 Bucket"
  skip_cleanup: true
  acl: public_read
  region: us-west-2
  upload-dir: www.scalabel.ai/doc
  on:
    branch: master
